#include <stdio.h>

void main() {
    int n=5,result;
    result=fact(n);
    printf("Factorial is %d",result);
}
int fact(int x)
{
    int i,fact=1;
    for(i=1;i<=x;i++)
    {
        fact=fact*i;
    }
    return(fact);
}
    
